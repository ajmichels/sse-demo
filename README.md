# Server-sent Events

Just playing around with this approach to HTTP requests.

## Running Locally

Run the server `pnpm install && pnpm serve`

Load the page `open http://localhost:3000`

*Or*

Run `docker compose up`
